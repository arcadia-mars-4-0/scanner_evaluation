## Scanner development staging config.
## Rebuild ./build first:
##   uv run python scanner_dev/ground_truth/build_dataset.py

transcripts: ./build/eval-logs
scans: ./scan-results

scanners:
- name: tool_access
  file: ../../scanners.py

validation:
  tool_access: ./dev_t2_validation.csv

model: openai/gpt-5.4
generate_config:
  max_connections: 50
